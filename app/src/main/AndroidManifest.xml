<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dzaky.fixisapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".profile.UserProfile"></activity>
        <activity android:name=".profile.Profile" />
        <activity android:name=".Login.LoginActivity" />
        <activity
            android:name=".instansi.Instansi"
            android:parentActivityName=".Login.MainActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity android:name=".sekolah.Sekolah" />
        <activity android:name=".toko.Toko" />
        <activity android:name=".masjid.Masjid" />
        <activity android:name=".Login.MainActivity" />
        <activity android:name=".Login.RegisterActivity" />
        <activity android:name=".Login.SpalashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>